% historyAction = Undo()
% Edit Action:
% 
% This is purely a placeholder to tell the replay action code that an undo
% was performed.

function historyAction = Undo()
    historyAction = 'Undo';
end
