% [historyAction] = AddHematoFluor()
% Edit Action:
% 
% This is just a dummy function so that the flourescence markers will be
% added to the undo stack.

function [historyAction] = AddHematoFluor(dummy)
    historyAction = 'Push';
end
